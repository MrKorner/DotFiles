{
  "layer": "top",
  "height": "40px",
  "position": "bottom",
  "modules-left": ["sway/workspaces"],
  "modules-center": ["sway/window"],
  "modules-right": ["sway/language", "custom/alsa", "temperature", "backlight", "battery", "clock",],
  "wlr/taskbar": {
    "format": "{icon} {title:.32}",
    "icon-size": 20,
    "icon-theme": "Flat-Remix-Red-Dark",
    "tooltip-format": "{title}",
    "on-click": "activate",
    "on-click-right": "minimize",
    "on-click-middle": "close",
  },
  "custom/alsa": {
    "exec": "amixer get -c PCH Master | awk '$0~/%/{print $4}' | tr -d '[]'",
    "on-click": "amixer -c PCH sset Master +1 toggle; pkill -x -RTMIN+11 waybar",
    "on-scroll-up": "amixer -c PCH sset Master 5%+; pkill -x -RTMIN+11 waybar",
    "on-scroll-down": "amixer -c PCH sset Master 5%-; pkill -x -RTMIN+11 waybar",
    "signal": 11,
    "interval": 10,
    "tooltip": false
  },

  "pulseaudio": {
    "tooltip": false,
    "on-click": "pavucontrol",
    "scroll-step": 5,
    "format": "{icon} ({volume}%)",
    "format-bluetooth": "{icon}BL ({volume}%)",
    "format-muted": "🔈 ({volume}%)",
    "format-icons": {
      "default": ["🔉", "🔊"],
    },
  },
  "battery": {
    "tooltip": false,
    "bat": "BAT0",
    "interval": 60,
    "states": {
      "warning": 30,
      "critical": 15,
    },
    "format": "🔋 ({capacity}%)"
  },
    "backlight": {
        "format": "{icon} {percent}%",
        "format-icons": {"default": ["🔅", "🔆"]},
        "on-scroll-up": "light -A 5",
        "on-scroll-down": "light -U 5",
  },
    "clock": {
        "interval": 1,
        "format": "{:%A %d %B %H:%M:%S}",
        "tooltip": true,
        "tooltip-format": "<big>{:%B %Y}</big>\n<tt>{calendar}</tt>"
  },
  "sway/workspaces": {
      "disable-scroll": false,
      "persistent_workspaces": true,
      "all-outputs": true,
      "format": "{name}",
        "1": "1",
        "2": "2",
        "3": "3",
        "4": "4",
  },  
  "sway/window": {
      "format": "{}",
      "max-length": 100,
  },
  "sway/language": {
      "format": "{}",
      "max-length": 50
  }
}
